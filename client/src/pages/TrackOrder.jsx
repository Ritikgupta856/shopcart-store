import StaticPageLayout from "@/components/StaticPageLayout";

const TrackOrder = () => (
  <StaticPageLayout title="Track Order">
    <p>Order tracking isn't available yet on the storefront — we're working on it.</p>
    <p>In the meantime, for questions about an existing order, please reach out via our Contact page with your order details.</p>
  </StaticPageLayout>
);

export default TrackOrder;
